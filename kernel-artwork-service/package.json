{
  "name": "kernel-artwork-service",
  "version": "0.1.0",
  "private": true,
  "description": "Backend proxy that resolves movie poster/backdrop artwork via TMDb (with an OMDb fallback), keeping API keys server-side.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
